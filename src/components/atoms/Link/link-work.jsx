import React from "react";
import { Button } from "@mui/material";
import './styles.scss';

const LinkWork = () => {
    return (
        <Button href="/" className="property">{'.work()'}</Button>
    );
}
 
export default LinkWork;