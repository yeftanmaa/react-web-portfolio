import React from "react";
import { Button } from "@mui/material";
import './styles.scss';

const LinkAbout = () => {
    return (
        <Button href="/" className="property">{'.about()'}</Button>
    );
}
 
export default LinkAbout;